int main(int argc,char **argv)
{
    int a[2];
    if(a[argc !=1])
        return 1;
    else
        return 0;
}
/*
the option in command line for compilation gcc <filename> -o <binnary name> -Wall -g -fsanitize=memory
doesn't work with gcc compiler 
*/